import React from 'react';
import styles from './Women.module.scss';

const Women = () => (
  <div className={styles.container}>
    <h1 className={styles.demographics}>WOMEN</h1>
  </div>
);

export default Women;
